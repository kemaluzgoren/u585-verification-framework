/**
 * @file    wifi_credentials.h.example
 * @brief   Template for wifi_credentials.h (access point credentials used
 *          by nx_driver_emw3080.c to connect). Copy this file to
 *          wifi_credentials.h in the same directory and fill in real
 *          values - wifi_credentials.h is gitignored so real credentials
 *          are never committed.
 *
 * @author  Kemal UZGOREN
 * @date    2026-08-28
 *
 * SPDX-License-Identifier: MIT
 */

#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

#define WIFI_SSID       "your-network-name"
#define WIFI_PASSWORD   "your-network-password"

#endif /* WIFI_CREDENTIALS_H */
